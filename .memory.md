# Summary

# Memory Log

## 2026-06-24T10:53:00Z

- **Agent observed** — The `provenance` skill (`src/enrich/provenance/SKILL.md`) was identified as "beautifully simple" through five principles:
  1. *Singular scope* — does exactly one thing (trace claims to sources) with explicit non-goals to prevent creep
  2. *Mirrors human intuition* — maps directly to the mental model of "where did you get that?" via a clean taxonomy of provenance types
  3. *Minimal classification* — four categories (`sourced`, `inferred`, `unsupported`, `contradicted`) cover the full universe without overlap
  4. *Default to minimal output* — returns sources only by default; excerpts only on request, making it composable with downstream steps
  5. *Graceful handoff* — when no source is found, offers concrete next steps (`lookup` or `remember`) instead of floundering
- **Agent observed** — The skill is ~60 lines of plain Markdown, structured as a natural interaction contract (prerequisites → inputs → processes → outputs → next steps → examples), with zero redundancy and no taxonomy bloat.
