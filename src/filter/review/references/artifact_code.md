# Artifact: Code

## Target Structure
Code review targets include:
- Source files (`.ts`, `.js`, `.py`, `.go`, etc.)
- Test files
- Code diffs (changed files)
- Configuration files (when relevant to the change)

## Target Metadata
- Lives in a code repository
- May be a single file, a directory, or a git diff
- Often accompanied by tests and documentation changes
